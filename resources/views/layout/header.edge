
@section('header')
    <div id='head' class='flex bg-gray-700 place-content-between text-white w-screen ' id='header'>
        <div class="inline px-4 py-4">
            <a href={{route('/')}}>
                <img class="h-12 w-12" src="spiked-dragon-head.png"> </img>
            </a>
        </div>
        <div class="inline px-4 py-4">
            @loggedIn
            <span> hello {{ auth.user.username }}</span>
            <a href={{route('logout')}}>
                <button class="btn-primary">Logout</button>
            </a>
            @endloggedIn
        </div>
    </div>
@endsection
